---
import Spotlight from "@/components/shared/molecules/Spotlight.astro";
import Card from "@/components/shared/molecules/Card/Card.astro";
import CardContent from "@/components/shared/molecules/Card/CardContent.astro";
import CardDescription from "@/components/shared/molecules/Card/CardDescription.astro";
import TagRow from "@/components/shared/atoms/TagRow.astro";
import Tag from "@/components/shared/atoms/Tag.astro";
import arrowRight from "@/svgs/arrow.svg?raw";
import { accentInlineStyle } from "@/lib/categoryColors";
import { formatDate, useTranslations, type Locale } from "@/i18n";

interface Props {
  title: string;
  subtitle?: string;
  date: Date;
  tags: string[];
  href: string;
  category?: string;
  lang?: Locale;
  contentLang?: Locale;
  isFallback?: boolean;
}

const {
  title,
  subtitle,
  date,
  tags,
  href,
  category,
  lang = "fr",
  contentLang = lang,
  isFallback = false,
} = Astro.props;

const t = useTranslations(lang);
const formattedDate = formatDate(date, lang);
const label = isFallback ? `${title} (${t.article.inOtherLanguage})` : title;
---

<a href={href} class="article-card-link" aria-label={label} style={accentInlineStyle(category)}>
  <Spotlight color="var(--accent-color)">
    <Card>
      <CardContent>
        <div slot="title" class="article-title">
          <div class="article-title-meta">
            <time datetime={date.toISOString()}>{formattedDate}</time>
            {isFallback && (
              <Tag title={contentLang.toUpperCase()} size="small" outline />
            )}
          </div>
          <h3>{title}<span class="arrow" aria-hidden="true" set:html={arrowRight} /></h3>
        </div>
        {subtitle && (
          <CardDescription>
            <p>{subtitle}</p>
          </CardDescription>
        )}
        {tags.length > 0 && (
          <TagRow class="article-card-tags">
            {tags.map((tag) => <Tag title={tag} />)}
          </TagRow>
        )}
      </CardContent>
    </Card>
  </Spotlight>
</a>

<style lang="scss">
  .article-card-link {
    text-decoration: none;
    display: block;
  }

  time {
    font-size: var(--font-size-xs);
    color: var(--text-muted);
  }

  .article-title-meta {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  /* Tag n'expose pas de prop de classe, le marqueur se regle donc depuis ici.
     Le selecteur doit rester global sur toute sa profondeur : le span est rendu
     par Tag et ne porte pas le scope de ce composant. */
  .article-title-meta :global(.tag) {
    padding: 2px 6px;
    /* Correction optique : moitie de l'ecart de hauteur de capitale entre le
       label de 11px et le texte de 15px a cote. Sans elle le label partage la
       ligne de base du voisin et sa masse visuelle tombe trop bas. */
    position: relative;
    top: -1.5px;
  }

  /* .small est indispensable ici : sans lui, la regle de Tag
     (div[cid].small span[cid]) est plus specifique et l'emporte. */
  .article-title-meta :global(.tag.small span) {
    font-size: var(--font-size-4xs);
    /* Sans ca le span herite de l'interligne de la carte et la pastille fait le
       double de la hauteur du texte de la date. */
    line-height: 1;
  }

  .article-card-tags {
    padding-top: 16px;
  }

  h3 {
    margin: 8px 0 0;

    .arrow {
      display: inline-flex;
      align-items: center;
      vertical-align: middle;
      margin-left: 8px;
      opacity: 0;
      transform: translateX(-4px) translateY(-0.1em);
      transition: all 0.2s ease;

      :global(svg) {
        width: 0.5em;
        height: 0.5em;
        fill: currentColor;
      }
    }
  }

  .article-card-link:hover h3 .arrow {
    opacity: 1;
    transform: translateX(0) translateY(-0.1em);
  }
</style>
