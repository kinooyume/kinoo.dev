---
import Tag from "../components/Tag.astro";
import React from "../svgs/react.svg?raw";
import Vue from "../svgs/vue.svg?raw";
import Svelte from "../svgs/svelte.svg?raw";
import Next from "../svgs/next.svg?raw";
import Rescript from "../svgs/rescript.svg?raw";
import Node from "../svgs/node.svg?raw";
import Ts from "../svgs/ts.svg?raw";
import Go from "../svgs/go.svg?raw";

type TagProps = {
  title: string;
  icon?: string;
};

// Stars
// https://codepen.io/bob6664569/pen/rOzmve
//
// Cut effect
// https://codepen.io/dtab428/pen/GmoMYG
//
// Moving letters
// https://tobiasahlin.com/moving-letters/?utm_source=bypeople
const techs: TagProps[] = [
  { title: "Typescript", icon: Ts },
  { title: "React.js", icon: React },
  { title: "Next.js", icon: Next },
  { title: "Vue.js", icon: Vue },
  { title: "Svelte", icon: Svelte },
  { title: "Rescript", icon: Rescript },
  { title: "Node.js", icon: Node },
  { title: "Golang", icon: Go },
  {title: "CI/CD"},
  { title: "... et plus !" },
];
---

<section id="hero">
  <div class="main">
    <div class="content">
      <div class="title">
        <span>Développeur Front-end/Fullstack</span>
        <h1>Martin Kinoo</h1>
      </div>
      <div class="description">
        <p>Hello ! 👋</p>
        <br />
        <p>
          Vous cherchez un développeur web pour votre prochain projet ? Je suis
          là !
        </p>
        <p>
          Avec plus de 4ans d’expérience en développement <b>front-end</b> et <b
            >fullstack</b
          >, je suis toujours à la recherche de nouvelles technologies à
          explorer et à les mettre en œuvre dans des projets concrets.
        </p>
        <p>
          Et quand je ne suis pas en train de coder, vous me trouverez
          probablement en train de tester un nouveau clavier ergonomique ⌨️ ou a
          dessiner 🎨.
        </p>
        <br />
        <p>Envie d'en savoir plus ? C'est par ici ! 👇</p>
      </div>
      <ul>
        {
          techs.map((tech) => (
            <li>
              <Tag title={tech.title} icon={tech.icon} />
            </li>
          ))
        }
      </ul>
    </div>
    <img class="image" src="images/profil2.png" alt="martin kinoo" />
  </div>
</section>

<style>
  :root {
    --hero-font-size: 20px;
    --hero-span-size: 36px;
  }
  .content {
    /* display: flex; */
    /* flex-direction: column; */
  }

  .main {
    display: flex;
    justify-content: space-between;
  }
  span {
    margin: 0;
    font-size: var(--hero-span-size);
    font-weight: 100;
    color: var(--accent-color);
  }

  h1 {
    font-size: var(--h1-size);
    line-height: 90px;
    margin: 0;
    font-weight: 200;
    color: var(--color);
  }

  .description p + p {
    margin-top: 8px;
  }
  ul {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
  .content {
    display: flex;
    flex-direction: column;
    gap: 64px;
    justify-content: space-between;
    max-width: 750px;
  }

  p {
    font-size: var(--hero-font-size);
    font-weight: 200;
  }

  #hero {
    margin-top: 32px;
  }

  img {
    margin: auto 0;
  }

  @media screen and (max-width: 1368px) {
    :root {
      --hero-font-size: 18px;
      --hero-span-size: 34px;
    }
  }
  @media screen and (max-width: 1145px) {
    img {
      display: none;
    }
  }

  @media screen and (max-width: 600px) {
    :root {
      --hero-span-size: 28px;
      --hero-font-size: 16px;
    }
    h1 {
      line-height: 1;
    }

    #hero {
      margin-top: 32px;
    }
    .content {
      gap: 64px;
    }
  }

  @media screen and (max-width: 450px) {
    :root {
      --hero-span-size: 24px;
    }
  }
</style>
