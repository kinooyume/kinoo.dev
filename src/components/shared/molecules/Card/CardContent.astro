<div class="card-content">
  <div class="title-wrapper" data-animate-elem>
    <div class="title" data-animate-elem>
      <slot name="title" />
    </div>
    <div class="marge" data-animate-elem>
      <slot name="marge" />
    </div>
  </div>
  <div class="body" data-animate-elem>
    <slot />
  </div>
</div>

<style>
  .card-content {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    --duration: 12s;
  }

  .title-wrapper {
    display: flex;
    justify-content: space-between;
    gap: clamp(14px, 0.76rem + 0.42vw, 18px);
    margin-bottom: clamp(14px, 0.58rem + 1.05vw, 24px);
  }

  .title {
    min-width: 0;
  }

  .marge {
    flex-shrink: 0;
  }

  .body {
    flex: 1;
    display: flex;
    flex-direction: column;
  }
</style>
