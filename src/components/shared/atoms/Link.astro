---
import externalLinkIcon from "@/svgs/external-link.svg?raw";

type Props = {
  href: string;
  text: string;
  external?: boolean;
  small?: boolean;
  class?: string;
};

const { href, text, class: className, external: forceExternal, small } = Astro.props;
const isExternal = forceExternal ?? href.startsWith("http");

const externalProps = isExternal
  ? { target: "_blank", rel: "noreferrer nofollow noopener" }
  : {};

const hasPrepend = Astro.slots.has("prepend");
const hasAppend = Astro.slots.has("append");
---

<a class:list={["link", className, { small }]} href={href} {...externalProps}>
  {hasPrepend && <span class="link-slot" aria-hidden="true"><slot name="prepend" /></span>}
  {text}
  {hasAppend && <span class="link-slot" aria-hidden="true"><slot name="append" /></span>}
  {isExternal && !hasAppend && <span aria-hidden="true"><Fragment set:html={externalLinkIcon} /></span>}
</a>

<style lang="scss">
  a {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: var(--font-size);
    color: var(--link-color, var(--accent-color, var(--light-color)));
    stroke: var(--link-color, var(--accent-color, var(--light-color)));
    opacity: var(--opacity-idle);
    transition: all var(--transition-base);
    text-decoration: underline;

    &:hover {
      opacity: 1;
      transform: translateY(-4px);
    }

    &:active {
      transform: translateY(5px);
      transition: var(--transition-snap);
    }

    &.small {
      font-size: var(--font-size-xs);
    }

    > span > :global(svg) {
      display: block;
      width: 0.75em;
      height: 0.75em;
    }
  }

  .link-slot {
    display: inline-flex;
    align-items: center;

    :global(svg) {
      width: 12px;
      height: 12px;
    }
  }
</style>
